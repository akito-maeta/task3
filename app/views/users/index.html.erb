<div class="container">
	<div class="row">

		<div class="col-xs-3">
			<h2>User info</h2>
			<%= attachment_image_tag @user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "100x100" %>
			<table>
				<tbody>
					<tr></tr>
					<tr>
						<th>name</th>
						<th><%= @user.name %></h3></th>
					</tr>
					<tr>
						<th width="112" height="37">introduction</th>
						<th width="100" height="37"><%= @user.introduction %></th>
					</tr>
				</tbody>
			</table>
			<%= link_to '',edit_user_path(@user), class: 'glyphicon glyphicon-wrench btn btn-default btn-block' %>

			<h2>New book</h2>
			<%= form_for(@book) do |f| %>
			<% if @book.errors.any? %>
			<div id="error_explanation">
				<h2><%= @book.errors.count %>error prohibited this obj from being saved:</h2>
				<ul>
					<% @book.errors.full_messages.each do |message| %>
					<li><%= message %></li>
					<% end %>
				</ul>
			</div>
			<% end %>
			<h5>Title</h5>
			<%= f.text_field :title %>
			<h5>Opinion</h5>
			<%= f.text_area :body %>
			<br>
			<%= f.submit 'Create Book', class: 'btn btn-primary btn-block' %>
			<% end %>
		</div>

		<div class="col-xs-9">
			<h2>Users</h2>
			<table>
				<thead>
					<tr>
						<th width="204" height="37">Image</th>
						<th width="331" height="37">name</th>
						<th width="162" height="37"></th>
					</tr>
				</thead>
				<tbody>
					<% @users.each do |user| %>
					<tr>
						<td><%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %></td>
						<td><%= user.name %></td>
						<td><%= link_to "Show" ,user_path(user.id) %></td>
					</tr>
					<% end %>
				</tbody>
			</table>
		</div>
	</div>
</div>